<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Profile</title>
  <!-- Bootstrap CSS -->
  <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #f8f9fa;
      padding-top: 2rem;
      padding-bottom: 2rem;
    }
    .profile-container {
      max-width: 600px;
      margin: 0 auto;
      padding: 25px;
      background-color: white;
      border-radius: 10px;
      box-shadow: 0 0.5rem 1rem rgba(0, 0, 0, 0.15);
    }
    .form-control:disabled {
      background-color: #f8f9fa;
      opacity: 1;
      font-weight: 500;
    }
    .profile-header {
      text-align: center;
      margin-bottom: 2rem;
      padding-bottom: 1rem;
      border-bottom: 1px solid #dee2e6;
    }
    .action-buttons {
      margin-top: 2rem;
      padding-top: 1.5rem;
      border-top: 1px solid #dee2e6;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="profile-container">
      <div class="profile-header">
        <h2>Profile</h2>
      </div>
      
      <% if (profile.length !== 0) { %>
        <form>
          <div class="mb-3 row">
            <label for="name" class="col-sm-3 col-form-label">Name</label>
            <div class="col-sm-9">
              <input type="text" class="form-control" id="name" name="name" value="<%= profile[0].name %>" disabled>
            </div>
          </div>
          
          <div class="mb-3 row">
            <label for="address" class="col-sm-3 col-form-label">Address</label>
            <div class="col-sm-9">
              <input type="text" class="form-control" id="address" name="address" value="<%= profile[0].address %>" disabled>
            </div>
          </div>
          
          <div class="mb-3 row">
            <label for="dateOfBirth" class="col-sm-3 col-form-label">Date of Birth</label>
            <div class="col-sm-9">
              <input type="date" class="form-control" id="dateOfBirth" name="dateOfBirth" value="<%= formatDate(profile[0].dateOfBirth) %>" disabled>
            </div>
          </div>
          
          <div class="mb-3 row">
            <label for="badge" class="col-sm-3 col-form-label">Badge</label>
            <div class="col-sm-9">
              <input type="text" class="form-control" id="badge" name="badge" value="<%= profile[0].badge %>" disabled>
            </div>
          </div>
        </form>
      <% } else { %>
        <div class="text-center p-4">
          <p class="mb-4">You haven't created a profile yet.</p>
          <a href="/profile/add" class="btn btn-primary">Add New Profile</a>
        </div>
      <% } %>
      
      <div class="action-buttons">
        <div class="d-flex flex-wrap justify-content-between">
          <div>
            <% if (profile.length !== 0) { %>
              <a href="/profile/edit" class="btn btn-warning me-2">Edit Profile</a>
              <a href="/profile/delete" class="btn btn-danger">Delete Profile</a>
            <% } %>
          </div>
          <div>
            <a href="/dashboard" class="btn btn-secondary">Back to Dashboard</a>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap Bundle with Popper -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>