<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Profile</title>
</head>
<body>

    <h1>Edit Profile</h1> <br>
        
    <form action="/profile/edit" method="post">
        <p>
            <label>Name</label>
            <input type="text" name="name" value="<%= profile[0].name %>">
        </p>

        <p>
            <label>Address</label>
            <input type="text" name="address" value="<%= profile[0].address %>">
        </p>

        <p>
            <label>Date of Birth</label>
            <input type="date" name="dateOfBirth" value="<%= formatDate(profile[0].dateOfBirth) %>">
        </p>

        <p>
            <label>Badge</label>
            <input type="text" name="badge" value="<%= profile[0].badge %>">
        </p>
        <input type="submit" value="Save">
    </form>
            
    <br>

    <a href="/dashboard">dashboard</a>
</body>
</html>