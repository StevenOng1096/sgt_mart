<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My App</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom styles -->
    <style>
        body, html {
            height: 100%;
        }
        .hero-section {
            padding: 5rem 0;
            background-color: #f8f9fa;
            border-radius: 0.5rem;
        }
        .btn-custom {
            margin-right: 0.5rem;
            margin-bottom: 0.5rem;
        }
        .center-content {
            display: flex;
            flex-direction: column;
            justify-content: center;
            min-height: calc(100vh - 140px); /* Adjusted for navbar and footer */
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">SGT Mart</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <% if (user) { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/dashboard">Dashboard</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/logout">Logout</a>
                        </li>
                    <% } else { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/login">Login</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/register">Register</a>
                        </li>
                    <% } %>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container center-content">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="hero-section text-center shadow-sm p-5">
                    <h1 class="display-4 mb-4">Welcome to SGT Mart</h1>
                    
                    <% if (user) { %>
                        <div class="alert alert-success" role="alert">
                            You are logged in as <strong><%= user.email %></strong>
                        </div>
                        <div class="mt-4">
                            <a href="/dashboard" class="btn btn-primary btn-lg btn-custom">
                                Dashboard
                            </a>
                            <a href="/logout" class="btn btn-outline-danger btn-lg btn-custom">
                                Logout
                            </a>
                        </div>
                    <% } else { %>
                        <p class="lead mb-4">Please login or register to see our products</p>
                        <div class="d-grid gap-2 d-md-block">
                            <a href="/login" class="btn btn-primary btn-lg btn-custom">
                                Login
                            </a>
                            <a href="/register" class="btn btn-success btn-lg btn-custom">
                                Register
                            </a>
                        </div>
                    <% } %>
                </div>
            </div>
        </div>
    </div>

    <footer class="bg-light text-center text-muted py-3 mt-auto">
        <div class="container">
            <p class="mb-0">© 2025 SGT Mart - All rights reserved</p>
        </div>
    </footer>

    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/bootstrap/5.3.2/js/bootstrap.bundle.min.js"></script>
</body>
</html>